/**
 * PunkORM — Example: Migrations
 *
 * Demonstrates the full lifecycle of migrations:
 *   - generate, run, rollback, flush
 *
 * Run commands via: bun bin/punk.ts <command>
 *   (or `npx punk` when installed as a package)
 */

// See punk.config.ts in project root — it points here to register entities.
// Generated migrations live in ./migrations/

export { };

